import React from 'react'

import { Box } from 'grommet'

import styled from 'styled-components'

const StyledFilter = styled(Box)`
  overflow: auto;
`

const MapFilter = () => {
  return <StyledFilter pad="medium">Filter</StyledFilter>
}

export default MapFilter
